import { defineMessages } from 'react-intl';

export default defineMessages({
  a11y_selectedFilter: {
    id: 'app.components.FilterSelector.a11y_selectedFilter',
    defaultMessage: 'Selected filter: ',
  },
});
