/*
 * Select Messages
 *
 * This contains all the text for the Select component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  noResults: {
    id: 'app.components.UI.Select.noResults',
    defaultMessage: 'No options available',
  },
});
