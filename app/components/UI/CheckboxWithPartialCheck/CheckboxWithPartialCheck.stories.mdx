import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import CheckboxWithPartialCheck from 'components/UI/CheckboxWithPartialCheck';
import { text, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';

<Meta title="CheckboxWithPartialCheck" component={CheckboxWithPartialCheck} />

# CheckboxWithPartialCheck

<Preview>
  <Story name="partially-checked">
    <CheckboxWithPartialCheck
      checked="mixed"
      onChange={action('checkbox clicked')}
    />
  </Story>
</Preview>

<Preview>
  <Story name="default">
    <CheckboxWithPartialCheck
      checked={boolean('Checked', true)}
      onChange={action('checkbox clicked')}
    />
  </Story>
</Preview>

<Preview>
  <Story name="with label">
    <CheckboxWithPartialCheck
      checked={boolean('Checked', true)}
      label={text('Label', 'A checkbox with label')}
      onChange={action('checkbox clicked')}
    />
  </Story>
</Preview>

<Props of={CheckboxWithPartialCheck} />
