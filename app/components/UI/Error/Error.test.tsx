import React from 'react';

// __tests__/hello_world.test.js
import { shallow } from 'enzyme';

import Error from './';

/*
describe('Error UI component', () => {
  it('renders', () => {
    const wrapper = shallow(<Error text="Hello Enzyme"/>);
    console.log(wrapper);
    expect(wrapper.find('h1').html()).toMatch(/Hello, Enzyme/);
  });
});
*/
