import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Checkbox from 'components/UI/Checkbox';
import { text, boolean } from "@storybook/addon-knobs";

<Meta title="Checkbox" component={Checkbox} />

# Checkbox

With `MDX` we write longform markdown documentation for our `Checkbox` component and embed Doc Blocks inline.

<Props of={Checkbox} />

<Preview>
  <Story name="default">
    <Checkbox checked={boolean('Checked', true)} />
  </Story>
</Preview>

<Preview>
  <Story name="with label">
    <Checkbox checked={boolean('Checked', true)} label={text('Label', 'This is a checkbox label')} />
  </Story>
</Preview>
