import styled from 'styled-components';
import { Grid } from 'semantic-ui-react';

const StyledContainerRow = styled(Grid.Row)`
  padding: 10px 0 0px 0 !important;
`;

export default StyledContainerRow;
