describe('<CommentEditor />', () => {
  it('should render the RTE', () => {
    // TOOO
    expect(true).toEqual(true);
  });

  it('should reset editor content if resetContent prop', () => {
    // TODO
    expect(true).toEqual(true);
  });

  describe('onEditorStateChange', () => {
    it('should pass on parentId to onEditorChange if parentId available', () => {
      // TOOO
      expect(true).toEqual(true);
    });
  });
});
