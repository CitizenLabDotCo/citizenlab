// import { fromJS } from 'immutable';
// import { makeSelectProjectsIndexPageDomain } from '../selectors';

// const selector = makeSelectProjectsIndexPageDomain();

describe('makeSelectProjectsIndexPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
