// import { fromJS } from 'immutable';
// import { makeSelectPagesShowPageDomain } from '../selectors';

// const selector = makeSelectPagesShowPageDomain();

describe('makeSelectPagesShowPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
