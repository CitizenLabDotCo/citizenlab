// Libraries
import * as React from 'react';
import * as Rx from 'rxjs/Rx';

// Services
import { observeProject, IProjectData } from 'services/projects';

// Component typing
type Props = {
};

type State = {
};

export default class AdminProjectEditEvents extends React.Component {
  render() {
    return (
      <div>Events tab</div>
    );
  }
}
