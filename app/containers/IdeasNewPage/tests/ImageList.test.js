// import React from 'react';
// import { shallow } from 'enzyme';

// import Attachment from '../index';

describe('<ImageList />', () => {
  it('should render the exact number of images + input', () => {
    // TODO
    expect(true).toEqual(true);
  });

  it('input button should opacity 0', () => {
    // TODO
    expect(true).toEqual(true);
  });
});
