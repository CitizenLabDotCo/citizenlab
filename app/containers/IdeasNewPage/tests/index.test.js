describe('index', () => {
  it('should not submit if title is not set', () => {
    expect(true).toEqual(true);
  });
  it('should not submit if content is not set', () => {
    expect(true).toEqual(true);
  });
});
