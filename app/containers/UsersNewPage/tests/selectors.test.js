// import { fromJS } from 'immutable';
// import { makeSelectUsersNewPageDomain } from '../selectors';

// const selector = makeSelectUsersNewPageDomain();

describe('makeSelectUsersNewPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
