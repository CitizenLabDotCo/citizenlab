// import { fromJS } from 'immutable';
// import { makeSelectIdeasPageDomain } from '../selectors';

// const selector = makeSelectIdeasPageDomain();

describe('makeSelectIdeasPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
