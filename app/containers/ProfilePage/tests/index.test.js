// import { mapDispatchToProps } from '../index';

describe('<ProfilePage />', () => {
  /*
   # TODO: TEST TO MAKE
   > REDUCER
   - should have initial state set properly
   - on LOAD_PROFILE action, loading should be set to true
   - on PROFILE_LOAD_SUCCESS action, loading should be set to false and userData not null
   - on PROFILE_STORE_SUCCESS action, processing should be set to false and userData not null

   > SAGAS
   - when LOAD_PROFILE action is received, either PROFILE_LOAD_SUCCESS or PROFILE_LOAD_ERROR actions should be dispatched
   - when STORE_PROFILE action is received, either PROFILE_STORE_SUCCESS or PROFILE_STORE_ERROR actions should be dispatched
   */
});
