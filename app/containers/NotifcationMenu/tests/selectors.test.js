// import { fromJS } from 'immutable';
// import { makeSelectNotifcationMenuDomain } from '../selectors';

// const selector = makeSelectNotifcationMenuDomain();

describe('makeSelectNotifcationMenuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
