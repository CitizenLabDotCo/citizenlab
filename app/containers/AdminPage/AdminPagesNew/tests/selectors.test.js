// import { fromJS } from 'immutable';

describe('AdminPages selectors', () => {
  it('it should have tests in the future', () => {
    expect(true).toEqual(true);
    // TODO: complete with selectors tests
  });
});
