import { randomString } from '../../../support/commands';

describe('Proposals with anonymous participation allowed - admin view', () => {
  beforeEach(() => {
    cy.setAdminLoginCookie();
  });

  it('temp', () => {});
});

describe('Proposals with anonymous participation allowed - normal user view', () => {
  beforeEach(() => {
    cy.setAdminLoginCookie();
  });

  it('temp', () => {});
});
