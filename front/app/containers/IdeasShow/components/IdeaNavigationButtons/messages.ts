import { defineMessages } from 'react-intl';

export default defineMessages({
  ideaIndexAriaMessage: {
    id: 'app.containers.IdeasShow.IdeaNavigationButtons.ideaIndexAriaMessage',
    defaultMessage: 'Viewing number {currentIndex} out of {totalInputs} inputs',
  },
});
