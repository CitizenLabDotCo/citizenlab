import { defineMessages } from 'react-intl';

export default defineMessages({
  goBackToSSOVerification: {
    id: 'app.containers.Authentication.steps.EmailAndPasswordVerifiedActions.goBackToSSOVerification',
    defaultMessage: 'Go back to single sign-on verification',
  },
});
