import { defineMessages } from 'react-intl';

export default defineMessages({
  search: {
    id: 'app.containers.Admin.projects.all.new.Folders.Filters.search',
    defaultMessage: 'Search folders',
  },
});
