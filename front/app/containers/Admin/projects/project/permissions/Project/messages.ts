import { defineMessages } from 'react-intl';

export default defineMessages({
  shouldProjectBeListed: {
    id: 'app.containers.Admin.projects.project.permissions.Project.shouldProjectBeListed',
    defaultMessage: 'Should the project be listed?',
  },
});
