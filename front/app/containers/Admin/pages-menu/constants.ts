import messages from './messages';

export const pagesAndMenuBreadcrumb = {
  label: messages.pagesAndMenuTitle,
  linkTo: '/pages-and-menu',
};
export const homeBreadcrumb = { label: messages.homeTitle, linkTo: '/home' };
