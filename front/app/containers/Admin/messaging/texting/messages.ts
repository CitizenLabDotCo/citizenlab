import { defineMessages } from 'react-intl';

export default defineMessages({
  invalidPhoneNumbers: {
    id: 'containers.Admin.messaging.texting.invalidPhoneNumbers',
    defaultMessage: 'One or more of the phone numbers are invalid.',
  },
});
