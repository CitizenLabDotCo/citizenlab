import { defineMessages } from 'react-intl';

export default defineMessages({
  events: {
    id: 'app.modules.commercial.analytics.admin.components.Events.events',
    defaultMessage: 'Events',
  },
});
