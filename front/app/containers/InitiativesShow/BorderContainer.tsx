import { defaultCardStyle } from '@citizenlab/cl2-component-library';
import styled from 'styled-components';

const BorderContainer = styled.div`
  margin-bottom: 45px;
  padding: 35px;
  border: 1px solid #e0e0e0;
  ${defaultCardStyle};
`;

export default BorderContainer;
