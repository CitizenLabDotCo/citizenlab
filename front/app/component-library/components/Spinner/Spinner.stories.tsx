import React from 'react';

import Spinner from './';

export default {
  title: 'Components/Spinner',
  component: Spinner,
};

export const Default = {
  render: () => <Spinner />,
  name: 'default',
};
