// in this case we want to import directly from the history library
// eslint-disable-next-line no-restricted-imports
import { createBrowserHistory } from 'history';

export default createBrowserHistory();
