import { defineMessages } from 'react-intl';

export default defineMessages({
  titleForAccessibility: {
    id: 'app.components.avatar.titleForAccessibility',
    defaultMessage: 'Profile of {fullName}',
  },
});
