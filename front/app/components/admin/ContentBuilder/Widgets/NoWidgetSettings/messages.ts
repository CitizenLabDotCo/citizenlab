import { defineMessages } from 'react-intl';

export default defineMessages({
  noSettings: {
    id: 'app.containers.admin.ContentBuilder.noSettings',
    defaultMessage: 'There are no settings for this widget.',
  },
});
