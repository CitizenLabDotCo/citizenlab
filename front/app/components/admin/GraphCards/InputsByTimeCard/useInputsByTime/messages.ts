import { defineMessages } from 'react-intl';

export default defineMessages({
  timeSeries: {
    id: 'app.components.admin.GraphsCards.PostsByTime.hooks.usePostsByTime.timeSeries',
    defaultMessage: 'Posts over time',
  },
});
