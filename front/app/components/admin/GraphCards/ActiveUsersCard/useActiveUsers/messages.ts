import { defineMessages } from 'react-intl';

export default defineMessages({
  timeSeries: {
    id: 'app.modules.commercial.analytics.admin.hooks.useParticipants.timeSeries',
    defaultMessage: 'Participants over time',
  },
});
