import { defineMessages } from 'react-intl';

export default defineMessages({
  timeSeries: {
    id: 'app.components.admin.GraphsCards.RegistrationsByTime.hooks.useRegistrationsByTime.timeSeries',
    defaultMessage: 'Registrations over time',
  },
});
