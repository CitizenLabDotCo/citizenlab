import { defineMessages } from 'react-intl';

export default defineMessages({
  timeSeries: {
    id: 'app.components.admin.GraphsCards.CommentsByTime.hooks.useCommentsByTime.timeSeries',
    defaultMessage: 'Comments over time',
  },
});
