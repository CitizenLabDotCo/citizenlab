import { defineMessages } from 'react-intl';

export default defineMessages({
  askFollowUpToggleLabel: {
    id: 'app.components.formBuilder.askFollowUpToggleLabel',
    defaultMessage: 'Ask follow up',
  },
});
