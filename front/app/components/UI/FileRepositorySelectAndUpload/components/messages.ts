import { defineMessages } from 'react-intl';

export default defineMessages({
  addFile: {
    id: 'app.components.FileRepositorySelectAndUpload.addFile2',
    defaultMessage: 'Select a file',
  },
});
