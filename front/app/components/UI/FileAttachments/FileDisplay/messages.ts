import { defineMessages } from 'react-intl';

export default defineMessages({
  ariaLabel: {
    id: 'app.components.UI.FileAttachments.FileDisplay.ariaLabel',
    defaultMessage: 'Download file {fileName}, {fileSize}',
  },
});
