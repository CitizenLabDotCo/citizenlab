import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import MoreActionsMenu from 'components/UI/MoreActionsMenu';
import { action } from '@storybook/addon-actions';
import Tippy from '@tippyjs/react';

<Meta title="MoreActionsMenu" component={MoreActionsMenu} />

# More Actions Menu

<Preview>
  <Story name="label">
    <div style={{ display: 'flex', justifyContent: 'center', width: '100%' }}>
      <MoreActionsMenu
        label="More Options"
        actions={[
          {
            label: 'Report as Spam',
            handler: action('Open spam report'),
          },
          {
            label: 'Edit idea',
            handler: action('Open idea edition'),
          },
          {
            label: 'Delete idea',
            handler: action('Delete idea'),
          },
        ]}
      />
    </div>
  </Story>
</Preview>

<Preview>
  <Story name="color">
    <div style={{ display: 'flex', justifyContent: 'center', width: '100%' }}>
      <MoreActionsMenu
        color="blue"
        actions={[
          {
            label: 'Report as Spam',
            handler: action('Open spam report'),
          },
          {
            label: 'Edit idea',
            handler: action('Open idea edition'),
          },
          {
            label: 'Delete idea',
            handler: action('Delete idea'),
          },
        ]}
      />
    </div>
  </Story>
</Preview>

<Preview>
  <Story name="minimal">
    <div style={{ display: 'flex', justifyContent: 'center', width: '100%' }}>
      <MoreActionsMenu
        actions={[
          {
            label: 'Report as Spam',
            handler: action('Open spam report'),
          },
          {
            label: 'Edit idea',
            handler: action('Open idea edition'),
          },
          {
            label: 'Delete idea',
            handler: action('Delete idea'),
          },
        ]}
      />
    </div>
  </Story>
</Preview>

<Props of={MoreActionsMenu} />
