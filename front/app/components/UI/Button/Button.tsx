import React from 'react';

// interface Props {
//   bgColor?: string;
// }

const Button = ({ bgColor }) => {
  return <button style={{ backgroundColor: bgColor }}>Bla</button>;
};

export default Button;
