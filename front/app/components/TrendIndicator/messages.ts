import { defineMessages } from 'react-intl';

export default defineMessages({
  previous_quarter: {
    id: 'app.components.trendIndicator.previous_quarter',
    defaultMessage: 'previous quarter',
  },
});
