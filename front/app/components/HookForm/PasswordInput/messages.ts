import { defineMessages } from 'react-intl';

export default defineMessages({
  passwordLabel: {
    id: 'app.components.HookForm.PasswordInput.passwordLabel',
    defaultMessage: 'Password',
  },
});
