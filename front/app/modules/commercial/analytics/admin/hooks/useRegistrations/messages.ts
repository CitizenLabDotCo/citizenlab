import { defineMessages } from 'react-intl';

export default defineMessages({
  timeSeries: {
    id: 'app.modules.commercial.analytics.admin.hooks.useRegistrations.timeSeries',
    defaultMessage: 'Registrations over time',
  },
});
