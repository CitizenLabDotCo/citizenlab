import { defineMessages } from 'react-intl';

export default defineMessages({
  timeSeries: {
    id: 'app.modules.commercial.analytics.admin.hooks.useActiveUsers.timeSeries',
    defaultMessage: 'Active users over time',
  },
});
