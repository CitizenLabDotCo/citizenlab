import React from 'react';

import { UserComponent } from '@craftjs/core';

const ImageTextCards: UserComponent = ({ children }) => {
  return <div>{children}</div>;
};

export default ImageTextCards;
