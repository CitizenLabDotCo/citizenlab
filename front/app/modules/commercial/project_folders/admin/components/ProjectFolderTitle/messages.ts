import { defineMessages } from 'react-intl';

export default defineMessages({
  projectsAndFolders: {
    id: 'app.containers.AdminPage.projects.all.adminPublications',
    defaultMessage: 'Projects and folders',
  },
});
