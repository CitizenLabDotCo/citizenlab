{
	"description": "The description preview of a project (max 280 characters)",
    "type": "object",
    "additionalProperties": false,
    "properties": {
     <%= I18n.available_locales.map{|l| "\"#{l.to_s}\": {\"type\": \"string\", \"maxLength\": 280}" }.join(",") %>
     } 
 }