# frozen_string_literal: true

## <% if @module_name.present? %><%= @module_name.camelize %>::<% end %><%= class_name %>Service.call
class <% if @module_name.present? %><%= @module_name.camelize %>::<% end %><%= class_name %>Service < ApplicationService
  def initialize(*args)
    # define initial state here
  end

  def call
    # service logic here, with access to initial state
  end

<% if methods.present? %>  private

  <% if methods.length == 1 %>def <%= methods.last %>; end<% else %><% methods[0..-2].each do |method| %>def <%= method %>; end<% end %>
  def <%= methods.last %>; end<% end %><% end %>
end
