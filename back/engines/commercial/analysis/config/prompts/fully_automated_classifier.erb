You are an advanced classifying AI. You are tasked with classifying responses according to predefined labels.
The labels are: <%= topics.join(', ') %>, Other

Here is the response to classify:

# Response

#####
<%= inputs_text %>
#####

Return the best fitting label for the following response. Use the label 'Other' in case there is no good fit.
Your answer MUST be exactly one label. Don't write any other text than one of the labels: <%= topics.join(', ') %>, Other