{
  "type" : "object",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "properties" : {
    "fields": {
      "anyOf" : [
        {
          "type" : "string",
          "pattern" : "^[a-z_]*\.[a-z_]*$"
        }, 
        {
          "type": "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[a-z_]*\.[a-z_]*$"
          },
          "minLength" : 1
        }
      ]
    },
    "fact" : {
      "type" : "string",
      "enum" : ["post", "participation", "visit"]
    },
    "filters" : {
      "type" : "object",
      "minProperties" : 1,
      "patternProperties" : {
        "^[a-z_]*$" : {
          "type" : "object",
          "minProperties" : 1,
          "patternProperties" : {
            "^[a-z_]*$" : {
              "type" : ["string", "array"],
              "items" : {"type" : "string"},
              "minLength" : 1,
              "additionalProperties": false
            },
            "date": {
              "type" : "object",
              "properties" : {
                "from" : {"type": ["string"]},
                "to" : {"type": ["string"]}
              },
              "required" : ["from", "to"]
            }
          }
        }
      },
      "additionalProperties": false
    },
    "groups" : {
      "anyOf" : [
        {
          "type" : "string",
          "pattern" : "^[a-z_]*\.[a-z_]*$"
        }, 
        {
          "type": "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[a-z_]*\.[a-z_]*$"
          },
          "minLength" : 1
        }
      ]
    },
    "aggregations" : {
      "type" : "object",
      "minProperties" : 1,
      "patternProperties" : {
        "^[a-z_]*\.[a-z_]*$" : {
          "anyOf" : [
            {
              "type" : "string",
              "enum" : ["min", "max", "avg", "sum", "count", "first"]
            }, 
            {
              "type": "array",
              "items" : {
                "type" : "string",
                "enum" : ["min", "max", "avg", "sum", "count", "first"]
              },
              "minLength" : 1
            }
          ]
        }
      },
      "additionalProperties" : false
    },
    "sort" : {
      "type": "object",
      "patternProperties" : {
        "^[a-z_]*\.[a-z_]*$" : {
          "type" : "string",
          "enum" : ["ASC", "DESC"]
        }
      },
      "additionalProperties" : false
    },
    "limit" : {
      "type" : "string"
    }
  },
  "additionalProperties" : false,
  "required": ["fact"],
  "oneOf": [
    {
        "required": [
            "fields"
        ]
    },
    {
        "required": [
            "aggregations"
        ]
    }
  ]
}
