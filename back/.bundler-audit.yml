---
ignore:
  - CVE-2026-38969 # webrick: transitive dependancy transitive of rackup 1.x, never loaded — Puma is the applicative server
  - GHSA-47m2-wp7j-p9vc # rubyzip < 3.4.0: the flawed prefix check is 2.4+/3.x code (Entry#extract_v3);
  # 1.3.0 has no such check. Both our extract sites validate the destination
  # themselves with a separator-terminated prefix guard. Revisit with the rubyzip 3 bump.
  - GHSA-42r3-x6vx-x49x # ruby_llm 1.16.0: ReDoS in the camelCase->snake_case regex.
  # The three call sites (tool.rb, agent.rb, chat.rb) only ever receive Ruby class
  # names, and we use neither Tool nor Agent. The advisory targets Ruby 3.1.x; the
  # regex is linear on our Ruby 3.4.7. The only patched release is 2.0.0.rc*, a
  # prerelease major. Revisit when 2.0 is stable or if a 1.16.x backport lands.
