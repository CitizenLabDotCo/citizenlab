import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import ListItem from './';
import Accordion from '../Accordion';
import { text } from '@storybook/addon-knobs';

<Meta title="Components/ListItem" component={ListItem} />

# ListItem

<ArgsTable of={ListItem} />

<Canvas>
  <Story name="default">
    <ListItem>{text('Text', 'Some text.')}</ListItem>
    <ListItem>{text('Text', 'Some text.')}</ListItem>
    <div style={{ padding: '10px' }}>Something in between</div>
    <ListItem>{text('Text', 'Some text.')}</ListItem>
    <ListItem>{text('Text', 'Some text.')}</ListItem>
  </Story>
</Canvas>

<Canvas>
  <Story name="combined with Accordion">
    <ListItem>{text('Text', 'Some text.')}</ListItem>
    <Accordion title={<>Title</>}>{text('Text', 'Some text.')}</Accordion>
    <div style={{ padding: '10px' }}>Something in between</div>
    <Accordion title={<>Title</>}>{text('Text', 'Some text.')}</Accordion>
    <ListItem>{text('Text', 'Some text.')}</ListItem>
  </Story>
</Canvas>
